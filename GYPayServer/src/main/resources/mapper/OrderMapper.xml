<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.wangcai.gypayserver.mapper.OrderMapper">
    <select id="getUnShipOrder" resultType="Order">
        select * from payorder right join payrecord on payrecord.order_id = payorder.order_id where account_name=#{account} and is_Ship=0 and time>date_sub(curdate(),interval 1 day);;
    </select>
    <select id="getShipOrder" resultType="Order">
        select * from payorder right join payrecord on payrecord.order_id = payorder.order_id where account_name=#{account} limit #{start},#{end};
    </select>
</mapper>