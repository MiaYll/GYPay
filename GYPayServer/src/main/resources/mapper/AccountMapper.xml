<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.wangcai.gypayserver.mapper.AccountMapper">
    <select id="getAccountInfo" resultType="Order">
        select * from payorder right join payrecord on payrecord.order_id = payorder.order_id where account_name=#{account} and is_Ship=1 and time >= #{start} and time &lt;= #{end};
    </select>
</mapper>